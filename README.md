the code in graph.cpp basically is an implementation of a class graph in c++ written by Dr.ibrahim Bluwi  
in the main function what i basically did is create two arrays one for densities that we need and an array of pairs for the size range of the graph were gonna generate see the doc file for more info  
Then it generates a simple undirected graph and reates a folder for each size range and creates 10 text files for each density category   
